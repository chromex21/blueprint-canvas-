/// Advanced Toolbar and Overlay Callbacks (exported from current simple_canvas.dart)
/// Provides callback signatures and helpers for toolbars, overlays, and tool options.

// Minimal typedefs for toolbar callbacks.
typedef OnShowTextToolOptions = void Function(dynamic anchor);
typedef OnConnectionSelected = void Function(dynamic connectionId);
typedef OnShapesDragging = void Function(dynamic shapeIds);
typedef OnEditorSelection = void Function();

// Add additional callback helpers and overlay logic here.
